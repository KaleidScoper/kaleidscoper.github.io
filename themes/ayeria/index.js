/**
 * Hexo 主题入口（必需的占位文件）
 *
 * Hexo 5.0+ 在运行 `hexo clean` 时会遍历主题目录并尝试加载 `index.js`。
 * 如果主题根目录缺少此文件，Hexo 会抛出 "Theme not loaded" 错误。
 * 此文件本身不注册任何 helper、filter 或 event——所有主题扩展逻辑
 * 分散在 `scripts/` 目录下的 helpers/、filters/、events/ 中，
 * Hexo 会自动加载 `scripts/` 目录内的所有 .js 文件。
 *
 * 参考: https://github.com/hexojs/hexo/issues/3820
 */